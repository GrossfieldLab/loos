# SConscript
# (c) 2008 Tod D. Romo
#
# Grossfield Lab
# Department fo Biochemistry & Biophysics
# University of Rochester Medical School
#
#


Import('env')



test_apps = 'atomic-group-test pdb-test parser-test princ_axes_tst timeseries-test find_perf_test pdb-shift'
list = []

for name in Split(test_apps):
    fname = name + '.cpp'
    prog = env.Program(fname)
    list.append(prog)

Return('list')
